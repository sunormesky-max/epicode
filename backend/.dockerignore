.git
.github
.vscode
.claude
target
data
*.env
.env*
*.md
ARCHITECTURE_CONTEXT.md
test_data_scheduler
sdk
# NOTE: do NOT ignore admin/ and docs/ — the cloud binary embeds them at
# compile time via include_str!("../../admin/index.html") and
# include_str!("../../docs/openapi.yaml"). Excluding them breaks the build.
