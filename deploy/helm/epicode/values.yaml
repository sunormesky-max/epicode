# Default values for Epicode Helm chart.
# Override these in your values.yaml or via --set.

replicaCount:
  backend: 1
  frontend: 2

image:
  backend:
    repository: ghcr.io/sunormesky-max/epicode-backend
    tag: "1.0.3"
    # 生产建议固定 SHA-256 digest(不可变引用, 免受 tag 漂移) — 设置后忽略 tag
    digest: ""            # e.g. "sha256:..."
    pullPolicy: IfNotPresent
  frontend:
    repository: ghcr.io/sunormesky-max/epicode-frontend
    tag: "1.0.3"
    digest: ""
    pullPolicy: IfNotPresent

# 密钥来源(审计 2026-09 中优 #15):
# 推荐 existingSecret 指向运维自建 Secret(external-secrets/密封), 密钥不进
# --set 的 shell 历史/CI 日志/helm release 记录; 留空且 env.*.ADMIN_KEY 非空时
# 才由 chart 显式创建 Secret(仅限开发环境)
externalSecret:
  existingSecret: ""

# Required secrets — provide via --set or external secret manager
env:
  EPICODE_ADMIN_KEY: ""        # REQUIRED
  EPICODE_MASTER_KEY: ""       # REQUIRED (32-byte base64)
  DEEPSEEK_API_KEY: ""         # optional
  REDIS_URL: ""                # optional, enables distributed rate limiting
  RUST_LOG: "info"
  EPICODE_LISTEN_ADDR: "0.0.0.0:9111"

service:
  backend:
    type: ClusterIP
    port: 9111
  frontend:
    type: ClusterIP
    port: 3000

ingress:
  enabled: true
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  hosts:
    - host: epicode.example.com
      paths:
        - path: /api
          pathType: Prefix
          service: backend
        - path: /docs
          pathType: Prefix
          service: backend
        - path: /health
          pathType: Exact
          service: backend
        - path: /
          pathType: Prefix
          service: frontend
  tls:
    - secretName: epicode-tls
      hosts:
        - epicode.example.com

persistence:
  enabled: true
  storageClass: ""
  accessMode: ReadWriteOnce
  size: 10Gi
  mountPath: /app/data

resources:
  backend:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: 1000m
      memory: 1Gi
  frontend:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi

probes:
  backend:
    liveness:
      httpGet:
        path: /health
        port: 9111
      initialDelaySeconds: 10
      periodSeconds: 30
    readiness:
      httpGet:
        path: /health
        port: 9111
      initialDelaySeconds: 5
      periodSeconds: 10

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80

redis:
  enabled: true
  image: redis:7-alpine
  persistence:
    enabled: false  # cache is rebuildable

nodeSelector: {}
tolerations: []
affinity: {}
